[tox]
envlist=py34-syntax, py27, py34

[testenv]
commands=
  nosetests -vs tests/static/
  nosetests -vs tests/live_tests/
deps=nose
passenv=TRAVIS

[testenv:py34-syntax]
whitelist_externals=bash
commands=bash -c "find pierky/ scripts/ tests/ -name "*.py" | xargs python -m py_compile"
basepython=python3.4
deps=
