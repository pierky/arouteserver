router id 99.0.2.33;

log "/var/log/bird.log" all;
log syslog all;
debug protocols all;

protocol device { }

protocol bgp the_rs {
	local as 131073;
	neighbor {{ data.rs_IPAddress }} as 999;
	import all;
	export none;
	connect delay time 1;
	connect retry time 1;
}
