router id {{ cfg.router_id }};
define rs_as = {{ cfg.rs_as }};

log "/var/log/bird.log" all;
log syslog all;
debug protocols { states, routes, filters, interfaces, events };

protocol device {};

{% if cfg.path_hiding -%}
{%   if "2.0.0"|target_version_le -%}
table master sorted;
{%   else -%}
ipv{{ip_ver}} table master{{ip_ver}} sorted;
{%   endif -%}
{%- endif %}
