router id {{ cfg.router_id }};
define rs_as = {{ cfg.rs_as }};
define default_local_pref = 100;
define rpki_invalid_local_pref = 80;

log "/var/log/bird.log" all;
log syslog all;
debug protocols all;

protocol device {};

{% if cfg.path_hiding -%}
table master sorted;
{%- endif %}
